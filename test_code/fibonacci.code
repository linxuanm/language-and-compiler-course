main() {
    decl a, b;
    a = 0;
    b = 1;

    while (b < 1000 && (TRUE || FALSE) && TRUE) {
        a = a + b;
        b = a - b;
        a = a - b;
        b = a + b;

        print(b + 0);
    }

    print("DONE");
}