main() {
    decl whilea, b;
    whilea = 0;
    b = 1;

    while (b < 1000 && (TRUE || FALSE) && TRUE) {
        whilea = whilea + b;
        b = whilea - b;
        whilea = whilea - b;
        b = whilea + b;

        print(b + 0);
    }

    print("DONE");
}